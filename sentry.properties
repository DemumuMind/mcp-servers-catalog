# Sentry CLI configuration
# Set SENTRY_AUTH_TOKEN via environment variable for CI/CD
# Run `sentry-cli login` for local development

defaults.url=https://sentry.io
defaults.org=
defaults.project=mcpservers-catalog
